---
description:
globs:
alwaysApply: true
---
# Git Branching Strategy для Krisp Notes Importer

## 🌳 Стратегия веток

### Основные ветки
- **`master`** - стабильная ветка для релизов
- **`develop`** - ветка разработки (опционально для крупных функций)
- **`feature/*`** - ветки для новых функций
- **`hotfix/*`** - ветки для критических исправлений

### Текущий подход: Simple Git Flow
Для небольшого проекта используем упрощенную модель:
- Основная разработка в `master`
- Ветки только для крупных функций или экспериментов
- Прямые коммиты в `master` для мелких изменений

## 🔄 Работа с ветками

### Создание ветки для новой функции
```bash
# Создать и переключиться на новую ветку
git checkout -b feature/advanced-search

# Или создать ветку от конкретного коммита
git checkout -b feature/new-ui master
```

### Работа в ветке
```bash
# Регулярные коммиты в ветке
git add .
git commit -m "Add search functionality"

# Отправка ветки в GitHub
git push origin feature/advanced-search
```

### Слияние с master
```bash
# Переключиться на master
git checkout master

# Обновить master
git pull origin master

# Слить ветку
git merge feature/advanced-search

# Удалить локальную ветку
git branch -d feature/advanced-search

# Удалить удаленную ветку
git push origin --delete feature/advanced-search
```

## 📋 Соглашения по именованию веток

### Типы веток:
- `feature/` - новые функции
- `bugfix/` - исправления ошибок
- `hotfix/` - критические исправления
- `docs/` - изменения документации
- `refactor/` - рефакторинг кода

### Примеры имен:
```
feature/localization-service
feature/file-watcher-improvements
bugfix/settings-not-saving
hotfix/critical-zip-parsing
docs/update-user-guide
refactor/processing-service
```

## 🚀 Сценарии использования веток

### 1. Мелкие изменения (прямо в master)
```bash
# Для небольших исправлений, обновлений документации
git add .
git commit -m "Fix typo in README"
git push origin master
```

### 2. Новая функция (отдельная ветка)
```bash
# Создать ветку
git checkout -b feature/export-functionality

# Разработка...
git add .
git commit -m "Add export to PDF functionality"

# Тестирование и доработка...
git add .
git commit -m "Fix export formatting issues"

# Слияние с master
git checkout master
git merge feature/export-functionality
git push origin master

# Очистка
git branch -d feature/export-functionality
```

### 3. Критическое исправление (hotfix)
```bash
# Создать hotfix ветку от master
git checkout -b hotfix/critical-crash-fix master

# Исправить проблему
git add .
git commit -m "Fix critical crash in ZIP processing"

# Слить в master
git checkout master
git merge hotfix/critical-crash-fix

# Создать релиз
git tag 3.3.2
git push origin master --tags

# Очистка
git branch -d hotfix/critical-crash-fix
```

## 🔍 Проверки перед слиянием

### Обязательные проверки:
- [ ] Код собирается без ошибок
- [ ] Все новые функции протестированы
- [ ] Нет конфликтов с master
- [ ] Коммиты имеют осмысленные сообщения
- [ ] Удалены отладочные console.log

### Команды для проверки:
```bash
# Проверить различия с master
git diff master

# Проверить историю коммитов
git log --oneline master..feature/branch-name

# Проверить статус
git status
```

## 🔄 Синхронизация с удаленным репозиторием

### Обновление локального master:
```bash
git checkout master
git pull origin master
```

### Обновление ветки функции:
```bash
git checkout feature/my-feature
git rebase master
# или
git merge master
```

### Отправка изменений:
```bash
# Отправить ветку
git push origin feature/my-feature

# Отправить master с тегами
git push origin master --tags
```

## ⚠️ Правила безопасности

### НИКОГДА НЕ ДЕЛАТЬ:
- ❌ Force push в master (`git push --force`)
- ❌ Rebase публичных веток
- ❌ Коммиты с несобирающимся кодом
- ❌ Слияние без тестирования

### ВСЕГДА ДЕЛАТЬ:
- ✅ Тестировать перед слиянием
- ✅ Использовать осмысленные имена веток
- ✅ Регулярно синхронизироваться с master
- ✅ Удалять ненужные ветки

## 📊 Мониторинг веток

### Полезные команды:
```bash
# Показать все ветки
git branch -a

# Показать ветки с последними коммитами
git branch -v

# Показать слитые ветки
git branch --merged

# Показать неслитые ветки
git branch --no-merged

# Очистить удаленные ветки
git remote prune origin
```

## 🎯 Рекомендации для проекта

### Когда использовать ветки:
- **Новые крупные функции** (>1 дня разработки)
- **Экспериментальные изменения**
- **Рефакторинг архитектуры**
- **Подготовка к major релизу**

### Когда коммитить прямо в master:
- **Мелкие исправления** (<1 часа)
- **Обновления документации**
- **Исправления опечаток**
- **Обновления версий для релиза**

## 🔧 Автоматизация

### GitHub Actions для веток:
- Автоматическая сборка при push в любую ветку
- Проверка TypeScript ошибок
- Уведомления о статусе сборки

### Защита master ветки:
- Требование успешной сборки перед слиянием
- Запрет force push
- Требование review для Pull Request (опционально)

Помните: **Простая стратегия веток = меньше проблем!**
