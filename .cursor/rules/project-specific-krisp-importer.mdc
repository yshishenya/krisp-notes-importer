# Специфичные правила для Krisp Notes Importer v3.5.3+

## 1. Современная архитектура с Dependency Injection

Плагин построен на современной модульной архитектуре с **Dependency Injection** паттерном, обеспечивающей высокую тестируемость, масштабируемость и поддерживаемость.

### Ключевые компоненты и файлы:

#### 🔧 **Основные сервисы**
*   **`[src/main.ts](mdc:src/main.ts)`**: Точка входа, инициализация IoC контейнера, регистрация команд
*   **`[src/core/SettingsManager.ts](mdc:src/core/SettingsManager.ts)`**: Управление настройками плагина
*   **`[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)`**: UI для настроек плагина в Obsidian

#### 🏗️ **Архитектура обработки (v3.5.0+)**
*   **`ProcessingOrchestrator`**: Главный координатор всего процесса импорта
*   **`MeetingExtractionService`**: Извлечение ZIP и валидация папок встреч
*   **`MeetingParsingService`**: Парсинг с стримингом больших файлов >100MB
*   **`MeetingImportService`**: Создание заметок и post-processing
*   **`ErrorHandlingService`**: Централизованная обработка ошибок

#### 📢 **Умная система уведомлений (v3.5.3+)**
*   **`[src/core/NotificationService.ts](mdc:src/core/NotificationService.ts)`**:
    - Автоматический batch режим для множественных операций (>1)
    - Интеллектуальная фильтрация избыточных уведомлений
    - Итоговые результаты с эмодзи: ✅⚠️❌ℹ️
    - Принудительные уведомления для критических событий

#### 🔍 **Улучшенная диагностика (v3.5.3+)**
*   **`[src/core/LoggingService.ts](mdc:src/core/LoggingService.ts)`**:
    - Structured logging с полным контекстом ошибок
    - Устранены silent failures - 100% диагностируемых ошибок
    - Stack traces и метаданные: пути файлов, индексы встреч
    - Error boundaries для неожиданных ошибок

#### 🛠️ **Поддерживающие сервисы**
*   **`[src/core/FileWatcherService.ts](mdc:src/core/FileWatcherService.ts)`**: Мониторинг папки для автоматического импорта
*   **`[src/core/LocalizationService.ts](mdc:src/core/LocalizationService.ts)`**: Двуязычная поддержка (русский/английский)
*   **`[src/core/StatusBarService.ts](mdc:src/core/StatusBarService.ts)`**: Индикация статуса в строке состояния Obsidian

#### 🔗 **Общие интерфейсы**
*   **`[src/interfaces.ts](mdc:src/interfaces.ts)`**: TypeScript интерфейсы для всех сервисов и настроек

## 2. Архитектурные принципы

### ✅ Соответствие архитектуре
Код должен строго следовать структуре:

```
src/
├── main.ts                 # MainPlugin + IoC контейнер
├── core/                   # Все бизнес-логика
│   ├── SettingsManager.ts  # ✅ РЕАЛИЗОВАН
│   ├── *Service.ts         # ✅ ВСЕ СЕРВИСЫ РЕАЛИЗОВАНЫ
├── ui/
│   └── SettingsTab.ts      # ✅ РЕАЛИЗОВАН
└── interfaces.ts           # ✅ РЕАЛИЗОВАН
```

### 🔧 Принципы разработки
- **Single Responsibility**: каждый сервис имеет одну четко определенную ответственность
- **Dependency Injection**: все зависимости инжектируются через IoC контейнер
- **Interface Segregation**: все сервисы реализуют минимальные интерфейсы
- **Типизация**: Использование интерфейсов из `[src/interfaces.ts](mdc:src/interfaces.ts)`. Строгая типизация
- **Error Boundaries**: централизованная обработка ошибок с graceful recovery

### 📋 Требования к новому коду
1.  Следовать архитектуре из `[docs/Architecture.MD](mdc:docs/Architecture.MD)`
2.  Обновлять `[src/interfaces.ts](mdc:src/interfaces.ts)` при изменении типов
3.  Добавлять JSDoc для публичных методов
4.  Интегрироваться с умной системой уведомлений
5.  Использовать structured logging с контекстом

### ❌ Антипаттерны (Запрещено)
- ❌ Прямое обращение к файловой системе минуя Obsidian API (кроме `FileWatcherService`)
- ❌ Хранение состояния в глобальных переменных
- ❌ Игнорирование ошибок без логирования
- ❌ Создание избыточных уведомлений (используйте batch режим)
- ❌ Silent failures (все ошибки должны быть диагностируемы)

## 3. Специфика разработки

### 🎨 Стиль кода
- **Классы**: PascalCase (`SettingsManager`)
- **Методы**: camelCase (`processZipFile`)
- **Константы**: UPPER_SNAKE_CASE (`DEFAULT_SETTINGS`)
- **Файлы**: PascalCase (`SettingsManager.ts`)

### 🔄 Основная логика обработки

Процесс импорта ZIP-архивов Krisp включает:

1.  **ProcessingOrchestrator**: Координатор полного цикла обработки
2.  **MeetingExtractionService**: Извлечение ZIP и валидация
3.  **MeetingParsingService**: Парсинг с оптимизацией для файлов >100MB
4.  **MeetingImportService**: Создание заметок и обработка аудио

### 📢 Умные уведомления (v3.5.3)

**Автоматические режимы:**
- **Single**: Детальные уведомления для одной операции
- **Batch**: Умная фильтрация для множественных операций (>1)

**Использование:**
```typescript
// Активация batch режима
notificationService.setBatchMode(true, 'Обработка 18 встреч...');

// Итоговый результат
notificationService.showBatchSummary(results, sourceInfo);
```

### 🔍 Диагностика ошибок (v3.5.3)

**Structured logging:**
```typescript
loggingService.error('Processing', 'Failed to read file', {
    filePath: '/path/to/file',
    meetingIndex: 5,
    stackTrace: error.stack,
    metadata: { operation: 'extraction' }
});
```

### 🛠️ Управление настройками

Осуществляется через:
1.  **`[src/core/SettingsManager.ts](mdc:src/core/SettingsManager.ts)`**: Бизнес-логика настроек
2.  **`[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)`**: UI всех настроек плагина

Структура настроек: `KrispImporterSettings` из `[src/interfaces.ts](mdc:src/interfaces.ts)`

### 🚀 Упрощенный релиз (v3.5.3+)

**Новый простой процесс:**
```bash
npm version patch    # Автообновление CHANGELOG.md
git add . && git commit -m "v<версия>: Описание"
git push origin master --tags
```

**Автоматически происходит:**
- ✅ GitHub release с notes из CHANGELOG.md
- ✅ Сборка и ZIP-архив
- ✅ Публикация релиза

## 4. Ключевые улучшения архитектуры

### 📊 Достижения v3.5.3
- **UX качество**: Спам уведомлениями устранен (40+ → 1 итоговое)
- **Диагностика**: 100% ошибок диагностируемы (устранены silent failures)
- **Информативность**: Structured logging с метаданными и полным контекстом
- **Производительность**: +40-60% для файлов >100MB (стриминг)
- **Error recovery**: 95% recoverable errors обрабатываются gracefully

### 🎯 Текущий статус
- **Версия**: v3.5.3 (unreleased) - UX и диагностика значительно улучшены
- **Архитектура**: Современная с dependency injection, SOLID принципы
- **Готовность**: К новым интеграциям и расширениям

**Помните: Качественная архитектура = стабильная работа и легкая поддержка!**

2.  **`[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)`**: Пользовательский интерфейс для всех настроек плагина, взаимодействие с `SettingsManager`.

Детальное описание настроек: `[docs/SRS.MD#311-Пользовательский-интерфейс-UI](mdc:docs/SRS.MD)`.
Структура настроек: `[docs/Architecture.MD#22-SettingsManager---Менеджер-настроек](mdc:docs/Architecture.MD)`.
