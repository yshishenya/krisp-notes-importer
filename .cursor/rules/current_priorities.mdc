---
description:
globs:
alwaysApply: true
---
# Текущие приоритеты Krisp Notes Importer

## 🎯 СТАТУС ПРОЕКТА: v2.0.0 - 65% готовности

Плагин **РАБОТАЕТ** для базового использования, но документация завышала возможности.

## ⚡ КРИТИЧЕСКИЕ ПРИОРИТЕТЫ

### 1. ИТЕРАЦИЯ 2.1.0 - Расширенный UI настроек (КРИТИЧНО)

**ПРОБЛЕМА**: Большинство настроек реализованы в `[src/interfaces.ts](mdc:src/interfaces.ts)` и используются в коде, но **НЕ ДОСТУПНЫ** через UI в `[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)`.

**ЧТО НУЖНО ДОБАВИТЬ В UI**:

1. **Стратегия дубликатов** (`duplicateStrategy`) - dropdown с опциями:
   - `'skip'` - Пропустить импорт
   - `'overwrite'` - Перезаписать существующий
   - `'rename'` - Создать с суффиксом (по умолчанию)

2. **Шаблоны имен**:
   - `noteNameTemplate` - формат имен заметок
   - `attachmentNameTemplate` - формат имен аудиофайлов
   - Добавить примеры и preview

3. **Опции после импорта** (checkboxes):
   - `deleteZipAfterImport` - удалять ZIP после импорта
   - `openNoteAfterImport` - открывать заметку после создания

4. **Настройка автосканирования**:
   - `autoScanEnabled` - checkbox (пока не работает, но нужен для v3.0.0)

5. **Шаблон содержимого**:
   - `noteContentTemplate` - большое textarea для редактирования
   - Кнопка "Восстановить по умолчанию"

6. **Язык**:
   - `language` - dropdown для локализации дат

### 2. ИТЕРАЦИЯ 3.0.0 - FileWatcherService (СРЕДНИЙ ПРИОРИТЕТ)

**ПРОБЛЕМА**: `[src/core/FileWatcherService.ts](mdc:src/core/FileWatcherService.ts)` НЕ СУЩЕСТВУЕТ, автоматическое отслеживание не работает.

**ЧТО НУЖНО**:
1. Создать файл `[src/core/FileWatcherService.ts](mdc:src/core/FileWatcherService.ts)`
2. Реализовать методы `startWatching()`, `stopWatching()`
3. Интегрировать в `[src/main.ts](mdc:src/main.ts)`
4. Добавить в UI переключатель автосканирования

## 📋 РАБОЧИЕ КОМПОНЕНТЫ (НЕ ТРОГАТЬ)

### ✅ ПОЛНОСТЬЮ РЕАЛИЗОВАНО И РАБОТАЕТ:
- `[src/main.ts](mdc:src/main.ts)` - основной класс плагина с командой импорта
- `[src/core/ProcessingService.ts](mdc:src/core/ProcessingService.ts)` - полная обработка ZIP
- `[src/core/NoteParser.ts](mdc:src/core/NoteParser.ts)` - расширенная аналитика
- `[src/core/NoteCreator.ts](mdc:src/core/NoteCreator.ts)` - создание заметок с callouts
- `[src/core/ZipExtractor.ts](mdc:src/core/ZipExtractor.ts)` - работа с архивами
- `[src/core/NotificationService.ts](mdc:src/core/NotificationService.ts)` - уведомления
- `[src/core/SettingsManager.ts](mdc:src/core/SettingsManager.ts)` - управление настройками
- `[src/interfaces.ts](mdc:src/interfaces.ts)` - все интерфейсы и настройки

### 🔄 ЧАСТИЧНО РАБОТАЕТ:
- `[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)` - только 3 базовых поля из 10+ настроек

## 🚫 НЕ РЕАЛИЗОВАННЫЕ ФУНКЦИИ

**НЕ ТРАТИТЬ ВРЕМЯ НА ЭТО СЕЙЧАС**:
- Мастер первоначальной настройки
- Тестовый импорт
- Массовый импорт
- Логирование
- Индикация статуса
- Интерактивные уведомления

## 🎯 ПЛАН ДЕЙСТВИЙ

### Шаг 1: Исправить UI настроек (КРИТИЧНО)
Расширить `[src/ui/SettingsTab.ts](mdc:src/ui/SettingsTab.ts)` до полной функциональности.

### Шаг 2: Создать FileWatcherService
Реализовать автоматическое отслеживание папки.

### Шаг 3: Обновить версию
Выпустить v2.1.0 с полным UI, затем v3.0.0 с автоматическим импортом.

**ПЛАГИН УЖЕ РАБОТАЕТ** для ручного импорта! Основная задача - довести UI до состояния функциональности.
