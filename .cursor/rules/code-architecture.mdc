---
description:
globs:
alwaysApply: true
---
# Архитектура кода Krisp Notes Importer

## 🏗️ Структура проекта

### Основные компоненты
- [main.js](mdc:main.js) - главный файл плагина (MainPlugin)
- [src/core/](mdc:src/core) - основная бизнес-логика
- [src/ui/](mdc:src/ui) - пользовательский интерфейс
- [src/interfaces.ts](mdc:src/interfaces.ts) - типы и интерфейсы

### Соответствие архитектуре
Код должен строго следовать структуре из [docs/Architecture.MD](mdc:docs/Architecture.MD):

```
src/
├── main.ts                 # MainPlugin
├── core/
│   ├── SettingsManager.ts  # ✅ РЕАЛИЗОВАН
│   ├── ProcessingService.ts # ✅ РЕАЛИЗОВАН
│   ├── ZipExtractor.ts     # ✅ РЕАЛИЗОВАН
│   ├── NoteParser.ts       # ✅ РЕАЛИЗОВАН
│   ├── NoteCreator.ts      # ✅ РЕАЛИЗОВАН
│   ├── NotificationService.ts # ✅ РЕАЛИЗОВАН
│   └── FileWatcherService.ts  # ❌ НЕ РЕАЛИЗОВАН
├── ui/
│   └── SettingsTab.ts      # ✅ ЧАСТИЧНО РЕАЛИЗОВАН
└── interfaces.ts           # ✅ РЕАЛИЗОВАН
```

## 🔧 Принципы разработки

### 1. Модульность
- Каждый сервис должен иметь четко определенную ответственность
- Взаимодействие только через публичные интерфейсы
- Минимальная связанность между компонентами

### 2. Типизация
- Использовать интерфейсы из [src/interfaces.ts](mdc:src/interfaces.ts)
- Строгая типизация для всех публичных методов
- Документирование сложных типов JSDoc

### 3. Обработка ошибок
- Все ошибки должны логироваться через [src/core/NotificationService.ts](mdc:src/core/NotificationService.ts)
- Graceful degradation при ошибках
- Информативные сообщения для пользователя

## 📋 Статус реализации компонентов

### ✅ Полностью реализованы:
- **SettingsManager** - управление настройками
- **ProcessingService** - обработка ZIP-файлов
- **ZipExtractor** - извлечение архивов
- **NoteParser** - парсинг данных Krisp
- **NoteCreator** - создание заметок Obsidian
- **NotificationService** - уведомления пользователя

### 🔄 Частично реализованы:
- **SettingsTab** - только базовые настройки в UI
- **MainPlugin** - основная логика есть, но нет FileWatcher

### ❌ Не реализованы:
- **FileWatcherService** - автоматическое отслеживание папки

## 🎯 Требования к новому коду

### При добавлении функций:
1. Следовать архитектуре из [docs/Architecture.MD](mdc:docs/Architecture.MD)
2. Обновлять [src/interfaces.ts](mdc:src/interfaces.ts) при изменении типов
3. Добавлять JSDoc для публичных методов
4. Тестировать интеграцию с существующими компонентами

### При изменении архитектуры:
1. **СНАЧАЛА** обновить [docs/Architecture.MD](mdc:docs/Architecture.MD)
2. Обновить план в [docs/DevelopmentPlan.MD](mdc:docs/DevelopmentPlan.MD)
3. Реализовать изменения в коде
4. Обновить документацию пользователя

## 🔍 Контроль качества кода

### Обязательные проверки:
- [ ] Код соответствует архитектуре
- [ ] Все публичные методы типизированы
- [ ] Ошибки обрабатываются корректно
- [ ] Нет дублирования логики
- [ ] Интерфейсы актуальны

### Интеграция с Obsidian API:
- Использовать только официальные API
- Обрабатывать асинхронные операции
- Корректно управлять жизненным циклом плагина
- Следовать best practices Obsidian

## 🚫 Антипаттерны

### Запрещено:
- ❌ Прямое обращение к файловой системе минуя Obsidian API
- ❌ Хранение состояния в глобальных переменных
- ❌ Игнорирование ошибок без логирования
- ❌ Реализация функций не по архитектуре
- ❌ Смешивание UI логики с бизнес-логикой

### Нежелательно:
- ⚠️ Большие методы (>50 строк)
- ⚠️ Глубокая вложенность (>3 уровня)
- ⚠️ Магические числа без констант
- ⚠️ Комментарии вместо понятного кода

## 📊 Метрики качества

### Архитектурные метрики:
- Соответствие реализации документации: **85%**
- Покрытие компонентов: **6/7 (86%)**
- Полнота интерфейсов: **100%**

### Технические метрики:
- Типизация публичных методов: **100%**
- Обработка ошибок: **95%**
- JSDoc покрытие: **70%**

## 🔄 Процесс рефакторинга

### При необходимости изменений:
1. Оценить влияние на архитектуру
2. Обновить документацию архитектуры
3. Создать план миграции
4. Реализовать изменения поэтапно
5. Обновить все связанные документы

### Приоритеты рефакторинга:
1. **Высокий**: Реализация FileWatcherService
2. **Средний**: Расширение SettingsTab UI
3. **Низкий**: Оптимизация производительности

## 🎨 Стиль кода

### TypeScript стандарты:
- Использовать `interface` для типов данных
- Использовать `class` для сервисов
- Предпочитать `const` и `readonly`
- Избегать `any`, использовать строгую типизацию

### Именование:
- **Классы**: PascalCase (`SettingsManager`)
- **Методы**: camelCase (`processZipFile`)
- **Константы**: UPPER_SNAKE_CASE (`DEFAULT_SETTINGS`)
- **Файлы**: PascalCase (`SettingsManager.ts`)

Помните: **Хорошая архитектура = поддерживаемый код!**
