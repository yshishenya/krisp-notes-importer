---
alwaysApply: true
description: "Общие рекомендации по ведению документации"
---


# Общие Рекомендации по Ведению Документации

## 🎯 Принцип: Минимум документов, максимум пользы

Для эффективной работы важно поддерживать документацию актуальной, краткой и полезной.

### 📂 Основные типы документов (могут варьироваться от проекта к проекту):
1.  **`README.md`**: Краткое описание проекта, инструкции по установке и быстрому старту. Целевая аудитория - новые пользователи и разработчики.
2.  **`CHANGELOG.md`**: История изменений проекта по версиям. Важно для отслеживания прогресса и понимания изменений между релизами. **АВТОМАТИЧЕСКИ используется для GitHub release notes.**
3.  **Требования к системе/продукту (например, `docs/SRS.md` или `docs/ProductRequirements.md`)**: Описывает, *что* система должна делать. Функциональные и нефункциональные требования.
4.  **Архитектура системы (например, `docs/Architecture.md`)**: Описывает, *как* система устроена. Ключевые компоненты, их взаимодействие, технологический стек.
5.  **Руководство пользователя (например, `docs/UserGuide.md`)**: Подробные инструкции по использованию всех функций продукта.
6.  **Бэклог/Планы развития (например, `docs/Backlog.md`)**: Список планируемых функций, улучшений и исправлений.

### ✍️ УПРОЩЕННЫЕ Правила обновления документации

**При каждом релизе АВТОМАТИЧЕСКИ происходит:**
1.  **`CHANGELOG.md` обновляется автоматически**: `npm version` команда заменяет `[Unreleased]` на `[версия] - дата`
2.  **GitHub release notes создаются автоматически** из соответствующей секции CHANGELOG.md
3.  **Версии обновляются автоматически** в `package.json`, `manifest.json` и других конфигурационных файлах

**Что нужно делать ВРУЧНУЮ перед релизом:**
1.  **КРИТИЧНО: Убедиться что секция `[Unreleased]` в CHANGELOG.md заполнена** со всеми важными изменениями
2.  **Проверить `README.md`**: Актуализировать инструкции по установке (если менялись)
3.  **Проверить ссылки**: Убедиться что все ссылки в документации работают

**При изменении архитектуры или добавлении значимых функций:**
1.  **СНАЧАЛА: Обновить `[Unreleased]` секцию в `CHANGELOG.md`** с детальным описанием изменений
2.  **Обновить документ с требованиями** (docs/SRS.md), если новая функция меняет или добавляет требования
3.  **Обновить документ с архитектурой** (docs/Architecture.md), если изменения затрагивают структуру компонентов или их взаимодействие
4.  **Обновить Руководство пользователя** (docs/UserGuide.md), если добавлена или изменена пользовательская функциональность

### 📝 Формат CHANGELOG.md (КРИТИЧНО для автоматических release notes)

```markdown
# 📝 Changelog

## [Unreleased] - 2025-XX-XX

### 🛠️ Улучшения
- Новая классная функция X
- Исправлено поведение Y

### 🐛 Исправления
- Починен баг Z

### 🔧 Технические изменения
- Рефакторинг компонента A

## [1.2.3] - 2025-01-15

### 🛠️ Улучшения
- Предыдущие изменения...
```

**ВАЖНО:**
- Секция `[Unreleased]` автоматически преобразуется в `[версия] - дата` при релизе!
- Используйте эмодзи для категоризации: 🛠️ 🐛 🔧 📚 ⚡ 🔒
- Описывайте изменения с точки зрения пользователя, а не кода

### 🔄 Контроль качества документации

**Автоматические проверки:**
-   **GitHub Actions** автоматически использует CHANGELOG.md для release notes
-   **Version scripts** автоматически обновляют версии во всех нужных файлах
-   **Build process** проверяет целостность документации при сборке

**Ручные проверки:**
-   **Перед каждым релизом**: Проверьте, что секция `[Unreleased]` содержит все важные изменения
-   **После релиза**: Убедитесь, что GitHub релиз содержит правильные release notes
-   **Регулярный аудит (раз в месяц)**: Просматривайте основные документы на предмет актуальности и полноты

### 🚨 Workflow интеграция документации и релизов

**Последовательность действий для надежного релиза:**

1. **Во время разработки**: Обновляйте `[Unreleased]` секцию по мере добавления функций
2. **Перед релизом**: Убедитесь что все изменения описаны в `[Unreleased]`
3. **Релиз**: Выполните `npm version patch/minor/major`
4. **Автоматически**: CHANGELOG.md обновится, версии синхронизируются
5. **Автоматически**: GitHub создаст релиз с release notes из CHANGELOG.md
6. **После релиза**: Проверьте что релиз создался корректно

**КРИТИЧЕСКИЕ принципы:**

-   **CHANGELOG.md как единственный источник истины**: Все release notes берутся отсюда
-   **Заполняйте [Unreleased] по ходу разработки**: Не оставляйте на последний момент
-   **Пишите для пользователей**: Объясняйте ЧТО изменилось, а не КАК это реализовано
-   **Проверяйте результат**: После релиза убедитесь что GitHub release notes корректны

### ⚠️ Критические принципы

-   **Честность**: Четко разделяйте реализованное и планируемое. Документируйте известные проблемы и ограничения
-   **Одна тема – один основной источник**: Если какая-то информация подробно описана в одном документе, другие документы должны ссылаться на него
-   **CHANGELOG.md как источник истины**: Эта секция используется для release notes, поэтому должна быть точной и информативной
-   **Автоматизация + контроль**: Используйте автоматизацию, но всегда проверяйте результат

### 🔗 Связь с процессом релизов

Эти правила документации тесно интегрированы с упрощенным процессом релизов:

- **npm version** автоматически обновляет CHANGELOG.md
- **GitHub Actions** извлекает release notes из CHANGELOG.md
- **Version scripts** синхронизируют версии во всех файлах
- **Ручная проверка** гарантирует качество итогового релиза

**Помните: Хорошая документация экономит время и силы всей команде и пользователям!**


Помните: **Хорошая документация экономит время и силы всей команде и пользователям!**

# Общие Рекомендации по Ведению Документации

## 🎯 Принцип: Минимум документов, максимум пользы

Для эффективной работы важно поддерживать документацию актуальной, краткой и полезной.

### 📂 Основные типы документов (могут варьироваться от проекта к проекту):
1.  **`README.md`**: Краткое описание проекта, инструкции по установке и быстрому старту. Целевая аудитория - новые пользователи и разработчики.
2.  **`CHANGELOG.md`**: История изменений проекта по версиям. Важно для отслеживания прогресса и понимания изменений между релизами. **АВТОМАТИЧЕСКИ используется для GitHub release notes.**
3.  **Требования к системе/продукту (например, `docs/SRS.md` или `docs/ProductRequirements.md`)**: Описывает, *что* система должна делать. Функциональные и нефункциональные требования.
4.  **Архитектура системы (например, `docs/Architecture.md`)**: Описывает, *как* система устроена. Ключевые компоненты, их взаимодействие, технологический стек.
5.  **Руководство пользователя (например, `docs/UserGuide.md`)**: Подробные инструкции по использованию всех функций продукта.
6.  **Бэклог/Планы развития (например, `docs/Backlog.md`)**: Список планируемых функций, улучшений и исправлений.

### ✍️ УПРОЩЕННЫЕ Правила обновления документации

**При каждом релизе АВТОМАТИЧЕСКИ происходит:**
1.  **`CHANGELOG.md` обновляется автоматически**: `npm version` команда заменяет `[Unreleased]` на `[версия] - дата`
2.  **GitHub release notes создаются автоматически** из соответствующей секции CHANGELOG.md
3.  **Версии обновляются автоматически** в `package.json`, `manifest.json` и других конфигурационных файлах

**Что нужно делать ВРУЧНУЮ перед релизом:**
1.  **КРИТИЧНО: Убедиться что секция `[Unreleased]` в CHANGELOG.md заполнена** со всеми важными изменениями
2.  **Проверить `README.md`**: Актуализировать инструкции по установке (если менялись)
3.  **Проверить ссылки**: Убедиться что все ссылки в документации работают

**При изменении архитектуры или добавлении значимых функций:**
1.  **СНАЧАЛА: Обновить `[Unreleased]` секцию в `CHANGELOG.md`** с детальным описанием изменений
2.  **Обновить документ с требованиями** (docs/SRS.md), если новая функция меняет или добавляет требования
3.  **Обновить документ с архитектурой** (docs/Architecture.md), если изменения затрагивают структуру компонентов или их взаимодействие
4.  **Обновить Руководство пользователя** (docs/UserGuide.md), если добавлена или изменена пользовательская функциональность

### 📝 Формат CHANGELOG.md (КРИТИЧНО для автоматических release notes)

```markdown
# 📝 Changelog

## [Unreleased] - 2025-XX-XX

### 🛠️ Улучшения
- Новая классная функция X
- Исправлено поведение Y

### 🐛 Исправления
- Починен баг Z

### 🔧 Технические изменения
- Рефакторинг компонента A

## [1.2.3] - 2025-01-15

### 🛠️ Улучшения
- Предыдущие изменения...
```

**ВАЖНО:**
- Секция `[Unreleased]` автоматически преобразуется в `[версия] - дата` при релизе!
- Используйте эмодзи для категоризации: 🛠️ 🐛 🔧 📚 ⚡ 🔒
- Описывайте изменения с точки зрения пользователя, а не кода

### 🔄 Контроль качества документации

**Автоматические проверки:**
-   **GitHub Actions** автоматически использует CHANGELOG.md для release notes
-   **Version scripts** автоматически обновляют версии во всех нужных файлах
-   **Build process** проверяет целостность документации при сборке

**Ручные проверки:**
-   **Перед каждым релизом**: Проверьте, что секция `[Unreleased]` содержит все важные изменения
-   **После релиза**: Убедитесь, что GitHub релиз содержит правильные release notes
-   **Регулярный аудит (раз в месяц)**: Просматривайте основные документы на предмет актуальности и полноты

### 🚨 Workflow интеграция документации и релизов

**Последовательность действий для надежного релиза:**

1. **Во время разработки**: Обновляйте `[Unreleased]` секцию по мере добавления функций
2. **Перед релизом**: Убедитесь что все изменения описаны в `[Unreleased]`
3. **Релиз**: Выполните `npm version patch/minor/major`
4. **Автоматически**: CHANGELOG.md обновится, версии синхронизируются
5. **Автоматически**: GitHub создаст релиз с release notes из CHANGELOG.md
6. **После релиза**: Проверьте что релиз создался корректно

**КРИТИЧЕСКИЕ принципы:**

-   **CHANGELOG.md как единственный источник истины**: Все release notes берутся отсюда
-   **Заполняйте [Unreleased] по ходу разработки**: Не оставляйте на последний момент
-   **Пишите для пользователей**: Объясняйте ЧТО изменилось, а не КАК это реализовано
-   **Проверяйте результат**: После релиза убедитесь что GitHub release notes корректны

### ⚠️ Критические принципы

-   **Честность**: Четко разделяйте реализованное и планируемое. Документируйте известные проблемы и ограничения
-   **Одна тема – один основной источник**: Если какая-то информация подробно описана в одном документе, другие документы должны ссылаться на него
-   **CHANGELOG.md как источник истины**: Эта секция используется для release notes, поэтому должна быть точной и информативной
-   **Автоматизация + контроль**: Используйте автоматизацию, но всегда проверяйте результат

### 🔗 Связь с процессом релизов

Эти правила документации тесно интегрированы с упрощенным процессом релизов:

- **npm version** автоматически обновляет CHANGELOG.md
- **GitHub Actions** извлекает release notes из CHANGELOG.md
- **Version scripts** синхронизируют версии во всех файлах
- **Ручная проверка** гарантирует качество итогового релиза

**Помните: Хорошая документация экономит время и силы всей команде и пользователям!**


Помните: **Хорошая документация экономит время и силы всей команде и пользователям!**
